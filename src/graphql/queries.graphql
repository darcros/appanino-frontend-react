query IsLoggedIn {
  isLoggedIn @client
}

query GetUserRole {
  userInfo @client {
    role
  }
}

query GetShopProducts {
  self {
    school{
      products{
				id
        name
        price
        category {
          name
        }
      }
    }
  }
}
