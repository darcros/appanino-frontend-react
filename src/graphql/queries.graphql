query IsLoggedIn {
  isLoggedIn @client
}

query GetUserRole {
  userInfo @client {
    role
  }
}

query GetShopProducts {
  self {
    school{
      products{
				id
        name
        price
        category {
          name
        }
      }
    }
  }
}

query userSettingsPageQuery {
  self {
    firstname
    lastname
    email
    school {
      id
      name
    }
  }
  
  schools {
    id
    name
  }
}
